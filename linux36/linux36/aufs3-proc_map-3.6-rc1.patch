--- aufs3-proc_map.patch~	2012-08-03 16:11:42.707689975 +0200
+++ aufs3-proc_map.patch	2012-08-03 21:30:23.995948420 +0200
@@ -91,15 +91,15 @@ diff --git a/mm/memory.c b/mm/memory.c
 index 2466d12..906bc23 100644
 --- a/mm/memory.c
 +++ b/mm/memory.c
-@@ -2658,6 +2658,8 @@ reuse:
- 		/* file_update_time outside page_lock */
- 		if (vma->vm_file)
- 			file_update_time(vma->vm_file);
-+		if (vma->vm_prfile)
-+			file_update_time(vma->vm_prfile);
- 
- 		return ret;
- 	}
+@@ -2653,6 +2653,8 @@ reuse:
+ 			/* file_update_time outside page_lock */
+ 			if (vma->vm_file)
+ 				file_update_time(vma->vm_file);
++			if (vma->vm_prfile)
++				file_update_time(vma->vm_prfile);
+ 		}
+ 		put_page(dirty_page);
+ 		if (page_mkwrite) {
 @@ -3343,6 +3345,8 @@ static int __do_fault(struct mm_struct *mm, struct vm_area_struct *vma,
  		/* file_update_time outside page_lock */
  		if (vma->vm_file)
